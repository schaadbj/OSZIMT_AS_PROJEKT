package application;

public class Menuswitch {
	public static void main(String[][] flugzeug) {
		int hauptmenu=IO_MainMenu.hauptmenu();
		switch (hauptmenu) {
			case 1 : //Objektverwaltung
				int untermenu=IO_MainMenu.menu_objverw();
				switch (untermenu) {
					case 1: //Objekt hinzufügen
						IO_Menu_objverw.neuobj(flugzeug);
					break;
					case 2: //Objekt entfernen
						IO_Menu_objverw.delobj(flugzeug);								//hier weitermachen
					break;
					case 3: //Ein Objekt anzeigen
						
					break;
					case 4: //Alle Objekte anzeigen
						
					break;
					case 0: //beenden
					break;
					default:
				}//end of switch untermenu
		break;
		case 2 :
		break;
		case 0: //beenden
		break;
		default:
		} //end of switch hauptmenu
	}
}
